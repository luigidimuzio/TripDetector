# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TripDetector' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TripDetector
  pod 'RealmSwift'

  target 'TripDetectorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TripDetectorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
